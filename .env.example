# Set these in Vercel Project Settings > Environment Variables.
# Vercel Postgres integration provides POSTGRES_URL and related POSTGRES_* values.
POSTGRES_URL=
JWT_SECRET=replace-with-a-long-random-secret
